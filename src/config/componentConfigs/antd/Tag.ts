import { PROPS_TYPES, ComponentConfig } from '@/types';


const Tag: ComponentConfig = {
  propsConfig: {
    color: {
      title: '标签色',
      type: PROPS_TYPES.string,
    },
  },
};

export default Tag;
